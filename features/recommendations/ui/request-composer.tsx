"use client";
import { Send } from "lucide-react";

export function RequestComposer({ value, onChange, onInterpret, onRecommend, hasDraft, loading }: { value: string; onChange: (value: string) => void; onInterpret: () => void; onRecommend: () => void; hasDraft: boolean; loading: boolean }) {
  return <div className="sticky bottom-4 pt-2"><div className="flex flex-col sm:flex-row sm:items-end gap-2 rounded-2xl border border-zinc-800 bg-zinc-900/90 p-2 backdrop-blur"><textarea value={value} onChange={(event) => onChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); onInterpret(); } }} rows={1} aria-label="Öneri isteği" placeholder={hasDraft ? "Takip isteği yaz: daha kısa olsun…" : "Ne izlemek veya okumak istediğini yaz"} className="min-w-0 flex-1 resize-none bg-transparent px-3 py-2 text-sm text-zinc-200 focus:outline-none" /><div className="flex gap-2"><button type="button" onClick={onInterpret} disabled={!value.trim() || loading} className="rounded-xl border border-zinc-700 px-3 py-2 text-xs text-zinc-300 disabled:opacity-40">İsteği çözümle</button><button type="button" onClick={onRecommend} disabled={!hasDraft || loading} className="flex items-center gap-1.5 rounded-xl border border-violet-500/40 bg-violet-500/20 px-3 py-2 text-xs font-medium text-violet-300 disabled:opacity-40"><Send className="h-4 w-4" />Önerileri bul</button></div></div></div>;
}
