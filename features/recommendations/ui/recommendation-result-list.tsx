"use client";
import type { AiRecommendation } from "@/lib/ai/types";
import { RecommendationResultCard } from "./recommendation-result-card";
export function RecommendationResultList({ recommendations, isAdded, isDismissed, onAdd, onDiscover, onSimilar, onDismiss, onRestore }: { recommendations: readonly AiRecommendation[]; isAdded: (rec: AiRecommendation) => boolean; isDismissed: (rec: AiRecommendation) => boolean; onAdd: (rec: AiRecommendation) => void; onDiscover: (rec: AiRecommendation) => void; onSimilar: (rec: AiRecommendation) => void; onDismiss: (rec: AiRecommendation) => void; onRestore: (rec: AiRecommendation) => void }) { return <div className="grid grid-cols-1 gap-3 md:grid-cols-2 2xl:grid-cols-3">{recommendations.map((rec) => <RecommendationResultCard key={rec.id} recommendation={rec} added={isAdded(rec)} dismissed={isDismissed(rec)} onAdd={() => onAdd(rec)} onDiscover={() => onDiscover(rec)} onSimilar={() => onSimilar(rec)} onDismiss={() => onDismiss(rec)} onRestore={() => onRestore(rec)} />)}</div>; }
