# MediaTracker Design References

This folder contains static design references, visual prototypes, and layout handoff notes. These files are not runtime application code and are not imported by the Next.js app.

Use them only as visual reference when comparing dashboard layout, world theming, spacing, or component direction. Generated exports and temporary dumps should stay under ignored subfolders such as `generated/` or `dump/`.
