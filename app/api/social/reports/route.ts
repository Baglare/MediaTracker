import { PRIVATE_NO_STORE_HEADERS, readJsonBody, safeSocialRouteError } from "@/lib/social/route-response";
import { safeSocialText, socialRecord, validateReportCategory, validateUuid } from "@/lib/social/interactions-validation";
import { getSupabaseServerClient } from "@/lib/supabase/server";

export const dynamic="force-dynamic";export const revalidate=0;
export async function POST(request:Request){const body=socialRecord(await readJsonBody(request));const activity=body?.activityId?validateUuid(body.activityId):null;const comment=body?.commentId?validateUuid(body.commentId):null;const category=validateReportCategory(body?.category);const note=safeSocialText(body?.note,500);if(!body||Number(Boolean(activity))+Number(Boolean(comment))!==1||(activity&&!activity.ok)||(comment&&!comment.ok)||!category.ok||!note.ok)return Response.json({message:!category.ok?category.error:!note.ok?note.error:"Rapor hedefi geçersiz."},{status:400,headers:PRIVATE_NO_STORE_HEADERS});try{const client=await getSupabaseServerClient();if(!client)throw new Error("social_not_configured");const {data,error}=await client.rpc("social_report",{p_activity:activity?.ok?activity.value:null,p_comment:comment?.ok?comment.value:null,p_category:category.value,p_note:note.value??null});if(error)throw new Error(error.message);return Response.json(data,{headers:PRIVATE_NO_STORE_HEADERS});}catch(error){return safeSocialRouteError(error);}}
